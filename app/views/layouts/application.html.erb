<!DOCTYPE html>
<html lang='en'>
  <head>
    <meta charset='utf-8' />
    <meta name='viewport' content='width=device-width, initial-scale=1.0' />

    <title><%= content_for?(:title) ? yield(:title) : 'Recouperator' %></title>

    <%= stylesheet_link_tag    'application' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <header class='top-bar'>
      <nav>
        <ul class="top-bar-right">
          <% if current_user %>
          <li class="col-md-1 col-sm-2 col-xs-12 nav-item"><%= link_to "Home", root_path, class: "link" %></li>
          <li class="col-md-1 col-sm-2 col-xs-12 nav-item"><%= link_to "My Profile", user_path(current_user), class: "link" %></li>
          <li class="col-md-1 col-sm-2 col-xs-12 nav-item"><%= link_to "Logout", destroy_user_session_path, method: "delete", class: "link" %></li>
          <% else %>
          <li><%= link_to "Sign Up", new_user_registration_path, class: "link" %></li>
          <li><%= link_to "Login", new_user_session_path, class: "link" %></li>
          <% end %>
        </ul>
      </nav>
    </header>
    <p class='notice'><%= notice %></p>
    <p class='alert'><%= alert %></p>

    <%= yield %>

    <footer class='navbar text-center'>
      <div class='container'>
        <div class=''>
          <h4>About</h4>
          <div class=''>
            <h5>Recouperator is a project created by Chris Prochnow.</h5>
          </div>
        </div>
        <div class=''>
          <h4>Contact</h4>
        <div class=''>
          <h4>Links</h4>
          <% if current_user %>
          <%= link_to 'My Profile', user_path(current_user), class: 'link' %><br>
          <%= link_to 'Logout', destroy_user_session_path, method: 'delete', class: 'link' %><br>
          <% else %>
          <%= link_to 'Sign Up', new_user_registration_path, class: 'link' %><br>
          <%= link_to 'Login', new_user_session_path, class: 'link' %><br>
          <% end %>
        </div>
      </div>
    </footer>

  </body>
</html>
